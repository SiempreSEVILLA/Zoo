<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    
    <% if (zoo && zoo.abteilungen) { %>
        <% zoo.abteilungen.forEach(e => { %>
            <li><%= e.name %></li>
        <% }) %>
    <% } else { %>
        <li>No departments found</li>
    <% } %>
</body>
</html>